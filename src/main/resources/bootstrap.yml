spring:
  application.name: techfood-app
  config.activate.on-profile: dev,prod
  main.banner-mode: off
  cloud:
    config:
      enabled: false
      fail-fast: false
      name: ${spring.application.name}

